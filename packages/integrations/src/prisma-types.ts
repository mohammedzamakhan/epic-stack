/**
 * Prisma type re-exports and fallbacks
 *
 * These types should ideally be imported from '@prisma/client',
 * but due to Prisma client generation issues, we define them here as fallbacks.
 */

// Define commonly used Prisma types as 'any' for now
// These should match the models in the Prisma schema
export type Integration = any
export type NoteIntegrationConnection = any
export type OrganizationNote = any
export type Organization = any
