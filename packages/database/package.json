{
  "type": "module",
  "name": "@repo/database",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": {
      "types": "./db.server.ts",
      "import": "./db.server.ts",
      "default": "./db.server.ts"
    },
    "./types": {
      "types": "./types.ts",
      "import": "./types.ts",
      "default": "./types.ts"
    }
  },
  "scripts": {
    "db:generate": "npx dotenv-cli -e ../../.env -- prisma generate --sql",
    "db:push": "npx dotenv-cli -e ../../.env -- prisma db push --skip-generate",
    "db:seed": "tsx seed.ts",
    "db:migrate:dev": "npx dotenv-cli -e ../../.env -- prisma migrate dev",
    "db:migrate:deploy": "npx dotenv-cli -e ../../.env -- prisma migrate deploy",
    "setup": "npm run db:migrate:deploy && npm run db:generate"
  },
  "dependencies": {
    "@epic-web/remember": "^1.1.0",
    "@prisma/client": "^6.18.0"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "@faker-js/faker": "^9.7.0",
    "prisma": "^6.8.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
