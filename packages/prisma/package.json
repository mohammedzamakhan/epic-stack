{
  "type": "module",
  "name": "@repo/prisma",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate --sql",
    "db:push": "prisma db push --skip-generate",
    "db:seed": "tsx seed.ts",
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "analytics:generate": "prisma generate --schema=schema-analytics.prisma",
    "analytics:push": "prisma db push --schema=schema-analytics.prisma --skip-generate",
    "analytics:migrate:dev": "prisma migrate dev --schema=schema-analytics.prisma",
    "analytics:migrate:deploy": "prisma migrate deploy --schema=schema-analytics.prisma",
    "setup": "npm run db:migrate:deploy && npm run db:generate && npm run analytics:migrate:deploy && npm run analytics:generate"
  },
  "dependencies": {
    "@epic-web/remember": "^1.1.0",
    "@prisma/client": "^6.18.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.7.0",
    "prisma": "^6.8.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
