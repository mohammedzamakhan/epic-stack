# Epic Stack - Marketing Website (Astro)
# This env file uses @env-spec - see https://varlock.dev/env-spec for more info
#
# NOTE: Astro uses import.meta.env for environment variables.
# PUBLIC_ prefix makes variables available in client-side code.
#
# @defaultRequired=infer @defaultSensitive=false
# @generateTypes(lang=ts, path=env.d.ts)
# ----------

# CMS Configuration
# @required @type=url @sensitive=false
PUBLIC_CMS_URL=https://cms.epic-startup.me:2999

# Root domain for cross-subdomain cookie sharing (used server-side)
# @sensitive=false
ROOT_APP=epic-startup.me

# Public root domain for client-side access (used in theme switcher)
# @sensitive=false
PUBLIC_ROOT_APP=epic-startup.me

# Preview Configuration - secret used to authenticate preview requests from CMS
# @required @sensitive
PREVIEW_SECRET=YOUR_SECRET_HERE

# BetterStack Uptime Monitoring Configuration (currently commented out in code)
# @docs(https://betterstack.com)
# @sensitive
BETTERSTACK_API_KEY=

# @type=url @sensitive=false
BETTERSTACK_URL=

# Node.js options (for development with self-signed certs)
# @sensitive=false
NODE_OPTIONS=--use-system-ca

# Trust self-signed certificates in development
# @type=number @sensitive=false
NODE_TLS_REJECT_UNAUTHORIZED=0

# @type=boolean
DEV=true
