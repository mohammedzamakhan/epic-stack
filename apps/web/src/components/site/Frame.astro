---
import { cn } from '../../lib/utils'

interface Props {
	class?: string
}

const { class: className = '' } = Astro.props
---

<div class="mx-auto w-full max-w-[1312px] px-4 sm:px-6">
	<div class={cn('relative border-x border-hairline', className)}>
		<slot />
	</div>
</div>
