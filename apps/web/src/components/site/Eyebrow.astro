---
import { cn } from '../../lib/utils'

interface Props {
	class?: string
}

const { class: className = '' } = Astro.props
---

<p class={cn('text-[15px] font-medium text-muted-foreground', className)}>
	<slot />
</p>
