---
import Blog from './Blog.astro'

interface Props {
	node: {
		title?: string
		subtitle?: string
	}
}

const { node = {} as any } = Astro.props
const { title, subtitle } = node
---

<Blog
	title={title}
	subtitle={subtitle}
	blockType="blog"
/>
