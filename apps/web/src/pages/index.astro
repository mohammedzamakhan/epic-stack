---
import MarketingLayout from '../layouts/MarketingLayout.astro'
import { getHomePage } from '../lib/emdash'
import RenderHero from '../components/RenderHero.astro'
import RenderBlocks from '../components/RenderBlocks.astro'
import MarketingBlocks from '../components/MarketingBlocks.astro'
import SEO from '../components/SEO.astro'
import { brand } from '@repo/config/brand'
import { stripMarkdown } from '../lib/markdown'

// Fetch home page content
const currentLocale = Astro.currentLocale ?? 'en'
const homePage = await getHomePage(currentLocale)

// SEO data - always available regardless of CMS content
const seoTitle = stripMarkdown(
	homePage?.seo?.title ||
		(homePage as any)?.meta?.title ||
		homePage?.title ||
		`${brand.name} - ${brand.tagline}`,
)
const seoDescription = homePage?.seo?.description || (homePage as any)?.meta?.description || brand.description
const seoImage = homePage?.seo?.image || (homePage as any)?.meta?.image
---

<MarketingLayout>
	<Fragment slot="head">
		<SEO
			title={seoTitle}
			description={seoDescription}
			image={seoImage}
		/>
	</Fragment>

	{/* If we have CMS content for home page, use it */}
	{
		homePage ? (
			<article>
				{/* Native Emdash PortableText sections */}
				{homePage.content && Array.isArray(homePage.content) && homePage.content.length > 0 ? (
					<MarketingBlocks value={homePage.content} />
				) : (
					<>
						{/* Render CMS Hero Section */}
						{homePage.hero && (
							<div>
								<RenderHero {...(homePage.hero as any)} />
							</div>
						)}

						{/* Render CMS Layout Blocks */}
						{homePage.layout && homePage.layout.length > 0 && (
							<RenderBlocks blocks={homePage.layout} />
						)}
					</>
				)}
			</article>
		) : (
			<main class="min-h-[60vh]">
				<section class="px-5 pt-28 pb-16 sm:px-8 sm:pt-32">
					<div class="mx-auto max-w-2xl text-center">
						<h1 class="font-display text-balance text-[40px] font-medium tracking-[-0.03em] text-foreground sm:text-[52px]">
							The {brand.name}
						</h1>
						<p class="mt-6 text-pretty text-[17px] leading-relaxed text-muted-foreground">
							Check the{' '}
							<a
								class="font-medium text-foreground underline-offset-4 hover:underline"
								href="https://github.com/mohammedzamakhan/epic-startup/blob/main/docs/getting-started.md"
							>
								Getting Started guide
							</a>{' '}
							to get your project off the ground.
						</p>
					</div>
				</section>
			</main>
		)
	}

	<!-- Blog Section -->
	<!-- {
		recentPosts.entries && recentPosts.entries.length > 0 && (
			<section class="relative w-full py-16 md:py-24">
				<GridDesign />
				<div class="container relative mx-auto px-4 md:px-6">
					<div class="text-center mb-12">
						<h3 class="font-mono text-sm uppercase tracking-wide text-muted-foreground mb-6">
							// LATEST INSIGHTS
						</h3>
						<h2 class="text-4xl md:text-6xl font-light text-primary mb-4">
							From our blog
						</h2>
						<p class="text-xl text-muted-foreground max-w-[600px] mx-auto">
							Stay updated with our latest insights, tutorials, and product
							updates
						</p>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-3 border-l border-t border-dashed gap-0">
						{recentPosts.entries.map((post, idx) => (
							<article class="border-b border-r border-dashed bg-background hover:bg-gradient-to-b from-muted/5 to-muted/50 transition-colors">
								{post.meta?.image && (
									<div class="aspect-video overflow-hidden border-b border-dashed">
										<img
											src={post.meta.image.url}
											alt={post.meta.image.alt || post.title}
											class="w-full h-full object-cover"
										/>
									</div>
								)}
								<div class="py-4 space-y-4">
									<h3 class="text-lg font-semibold text-primary border-l-2 border-primary pl-4">
										<a
											href={`/posts/${post.slug}`}
											class="hover:text-primary/80 transition-colors"
											set:html={parseMarkdownInline(post.title)}
										/>
									</h3>
									{post.meta?.description && (
										<p
											class="text-muted-foreground text-sm leading-relaxed pl-4"
											set:html={parseMarkdownBlock(post.meta.description)}
										/>
									)}
									<a
										href={`/posts/${post.slug}`}
										class="inline-flex items-center text-sm font-medium text-primary hover:text-primary/80 border-b-2 border-transparent hover:border-primary/80 transition-colors pl-4"
									>
										Read more
										<svg
											class="ml-1 w-4 h-4"
											fill="none"
											stroke="currentColor"
											viewBox="0 0 24 24"
											xmlns="http://www.w3.org/2000/svg"
										>
											<path
												stroke-linecap="round"
												stroke-linejoin="round"
												stroke-width="2"
												d="M17 8l4 4m0 0l-4 4m4-4H3"
											/>
										</svg>
									</a>
								</div>
							</article>
						))}
					</div>

					<div class="text-center mt-12">
						<a
							href="/posts"
							class="inline-flex items-center text-lg font-medium text-primary hover:text-primary/80 border-b-2 border-transparent hover:border-primary/80 transition-colors"
						>
							View all posts
							<svg
								class="ml-2 w-4 h-4"
								fill="none"
								stroke="currentColor"
								viewBox="0 0 24 24"
								xmlns="http://www.w3.org/2000/svg"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									stroke-width="2"
									d="M17 8l4 4m0 0l-4 4m4-4H3"
								/>
							</svg>
						</a>
					</div>
				</div>
			</section>
		)
	} -->
</MarketingLayout>
