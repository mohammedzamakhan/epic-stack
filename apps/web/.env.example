LITEFS_DIR="/litefs/data"
DATABASE_PATH="../../packages/prisma/db/data.db"
DATABASE_URL="file:./db/data.db?connection_limit=1"
CACHE_DATABASE_PATH="./other/cache.db"
SESSION_SECRET="super-duper-s3cret"
HONEYPOT_SECRET="super-duper-s3cret"
RESEND_API_KEY="re_blAh_blaHBlaHblahBLAhBlAh"
SENTRY_DSN="your-dsn"
BASE_URL=http://localhost:3001

STRIPE_SECRET_KEY=sk_test_***
STRIPE_WEBHOOK_SECRET=whsec_***
STRIPE_PORTAL_URL="https://billing.stripe.com/p/login/test_***"
TRIAL_DAYS=14
CREDIT_CARD_REQUIRED_FOR_TRIAL="manual" # stripe or manual
LAUNCH_STATUS="LAUNCHED" # CLOSED_BETA, PUBLIC BETA, LAUNCHED

# this is set to a random value in the Dockerfile
INTERNAL_COMMAND_TOKEN="some-made-up-token"

# the mocks and some code rely on these two being prefixed with "MOCK_"
# if they aren't then the real github api will be attempted
GITHUB_CLIENT_ID="MOCK_GITHUB_CLIENT_ID"
GITHUB_CLIENT_SECRET="MOCK_GITHUB_CLIENT_SECRET"
GITHUB_TOKEN="MOCK_GITHUB_TOKEN"
GITHUB_REDIRECT_URI="https://example.com/auth/github/callback"

# set this to false to prevent search engines from indexing the website
# default to allow indexing for seo safety
ALLOW_INDEXING="true"

# Tigris Object Storage (S3-compatible) Configuration
AWS_ACCESS_KEY_ID="mock-access-key"
AWS_SECRET_ACCESS_KEY="mock-secret-key"
AWS_REGION="auto"
AWS_ENDPOINT_URL_S3="https://fly.storage.tigris.dev"
BUCKET_NAME="mock-bucket"


# Jira Integration OAuth Configuration
# Get these from https://developer.atlassian.com/cloud/jira/platform/rest/api-group-projects/#api-rest-api-3-project-projectIdOrKey-issues-post
JIRA_CLIENT_ID="your-jira-client-id"
JIRA_CLIENT_SECRET="your-jira-client-secret"

# Slack Integration OAuth Configuration
# Get these from https://api.slack.com/authentication/oauth-v2
SLACK_CLIENT_ID="your-slack-client-id"
SLACK_CLIENT_SECRET="your-slack-client-secret"

# Linear Integration OAuth Configuration
# Get these from https://linear.app/settings/api
LINEAR_CLIENT_ID="your-linear-client-id"
LINEAR_CLIENT_SECRET="your-linear-client-secret"

# GitLab Integration OAuth Configuration
# Get these from https://gitlab.com/-/profile/applications (or your GitLab instance)
GITLAB_CLIENT_ID="your-gitlab-client-id"
GITLAB_CLIENT_SECRET="your-gitlab-client-secret"

# ClickUp Integration OAuth Configuration
# Get these from https://app.clickup.com/apps (create a new OAuth app)
CLICKUP_CLIENT_ID="your-clickup-client-id"
CLICKUP_CLIENT_SECRET="your-clickup-client-secret"

# Notion Integration OAuth Configuration
# Get these from https://www.notion.so/my-integrations (create a new integration)
NOTION_CLIENT_ID="your-notion-client-id"
NOTION_CLIENT_SECRET="your-notion-client-secret"

# Asana Integration OAuth Configuration
# Get these from https://app.asana.com/0/developer-console (create a new app)
ASANA_CLIENT_ID="your-asana-client-id"
ASANA_CLIENT_SECRET="your-asana-client-secret"

# Trello Integration OAuth Configuration
# Get these from https://trello.com/app-key
TRELLO_API_KEY="your-trello-api-key"
TRELLO_API_SECRET="your-trello-api-secret"

# GitHub Integration OAuth Configuration
# Get these from https://github.com/settings/applications/new (create a new OAuth app)
# Note: These are separate from the existing GITHUB_CLIENT_ID/SECRET used for authentication
GITHUB_INTEGRATION_CLIENT_ID="your-github-integration-client-id"
GITHUB_INTEGRATION_CLIENT_SECRET="your-github-integration-client-secret"

# Trigger.dev Configuration
TRIGGER_PROJECT_ID="your-trigger-project-id"
TRIGGER_SECRET_KEY="your-trigger-secret-key"