// This module contains type definitions for modules which do not have
// their own type definitions and are not available on DefinitelyTyped.

// Example:
// declare module 'some-untyped-pkg' {
// 	export function foo(): void;
// }

// Placeholder to prevent empty file warning
declare global {
	// Add global type augmentations here if needed
}

export {}
