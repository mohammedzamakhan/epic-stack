# Run on each regional OCI VM (Ashburn for us, Riyadh for ksa).
# Copy .env.example to .env on the host and set TENANT_API_IMAGE after CI pushes.

services:
  tenant-api:
    image: ${TENANT_API_IMAGE:-tenant-api}
    restart: unless-stopped
    ports:
      - '8080:8080'
    env_file: .env
    environment:
      PORT: '8080'
      TENANT_DB_DIR: /data/tenants
    volumes:
      - /data/tenants:/data/tenants
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:8080/health').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
