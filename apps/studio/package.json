{
  "name": "studio",
  "version": "0.0.0",
  "scripts": {
    "dev": "DATABASE_URL='file:./db/data.db?connection_limit=1' prisma studio --schema ../../packages/database/schema.prisma --port 3003 --browser none",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "devDependencies": {
    "prisma": "^6.8.2"
  }
}
